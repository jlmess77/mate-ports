--- Makefile.am.orig	2012-07-16 15:23:42.000000000 -0500
+++ Makefile.am	2012-07-16 15:24:13.000000000 -0500
@@ -72,7 +72,6 @@
 # we don't want to install mo files, all translations are already stored
 # in theme files
 install-data-local:
-	$(MAKE) -C po uninstall
 	if [ -h $(DESTDIR)$(themedir) ]; then \
 		rm -f $(DESTDIR)$(themedir); \
 	fi
